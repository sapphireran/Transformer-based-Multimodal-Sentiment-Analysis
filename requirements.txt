# Runtime for the original training / evaluation scripts.
# CPU wheels are enough for the examples and tests in this repo.
torch>=2.0,<3
numpy>=1.23
scikit-learn>=1.2
scipy>=1.10
tqdm>=4.65
memory-profiler>=0.61
matplotlib>=3.7
h5py>=3.8

# Optional: only needed if you rebuild CMU-MOSI / CMU-MOSEI from the SDK.
# mmsdk
# torchtext

# Docs / example test extras
pytest>=7.4
