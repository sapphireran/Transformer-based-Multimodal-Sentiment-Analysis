# Personal research stack for GMTM / fusion baselines.
# Train and evaluate on a GPU when available; CPU is enough for the
# synthetic examples under examples/.

torch>=2.1
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
tqdm>=4.66
matplotlib>=3.8
memory-profiler>=0.61
pytest>=8.0
