# Core training / inference
torch>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
tqdm>=4.65

# Optional: memory / plot utilities used by the original training loop
memory-profiler>=0.61
matplotlib>=3.7

# Optional: building MOSI / MOSEI pickles from CMU-MultimodalSDK
# h5py>=3.8
# mmsdk  # install from https://github.com/A2Zadeh/CMU-MultimodalSDK

# Docs / examples extras
pytest>=8.0
