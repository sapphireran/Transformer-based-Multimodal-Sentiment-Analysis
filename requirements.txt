# Personal research stack for Transformer-based Multimodal Sentiment Analysis.
# CPU wheels are enough for the synthetic examples in examples/.
# Full CMU-MOSI / CMU-MOSEI training additionally needs CUDA, mmsdk, and the raw feature files.

torch>=2.1.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
tqdm>=4.66.0
matplotlib>=3.8.0
memory-profiler>=0.61.0

# Optional: only required when rebuilding pickles from CMU-MultimodalSDK .csd files
# h5py>=3.10.0
# mmsdk
# torchtext
