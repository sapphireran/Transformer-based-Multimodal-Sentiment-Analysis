# Core training stack used by the original experiment scripts.
# Install a CPU or CUDA PyTorch build that matches your machine:
#   https://pytorch.org/get-started/locally/
torch>=2.1
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
tqdm>=4.66
matplotlib>=3.8
memory-profiler>=0.61

# Optional: only needed to rebuild CMU-MOSI / CMU-MOSEI pickles from .csd files.
# mmsdk
# h5py
# torchtext
